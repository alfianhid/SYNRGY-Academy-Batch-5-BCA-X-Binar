## PostgreSQL DataSource
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database=postgresql
spring.datasource.url=jdbc:postgresql://containers-us-west-198.railway.app:5967/railway
spring.datasource.username=postgres
spring.datasource.password=mErkOkbRadpSX2GVAAJh

## JPA / Hibernate
spring.jpa.generate-ddl=true
spring.jpa.show-sql=false
spring.jpa.hibernate.show-sql=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# To format SQL queries generated by Hibernate
spring.jpa.properties.hibernate.format_sql=true

# Jackson timestamp
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
spring.jackson.time-zone=GMT+7

# change the port number
server.port=9192

# change Spring Boot logo
spring.banner.location=classpath:banner.txt

# swagger docs
springdoc.swagger-ui.defaultModelsExpandDepth=-1

# logging
logging.level.root = info

# JWT configuration
jwtSecret=superSecretKey
jwtExpirationMs=86400000
